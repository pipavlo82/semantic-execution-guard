# Local relation demo

`node demo/relation-demo.mjs` emits bad and good semantic paths as JSON.
Both are SYNTHETIC_RELATION_FIXTURE. Expected future guard behavior is labelled
separately from actual execution, which is NOT_IMPLEMENTED_NOT_OBSERVED.
No frontend, RPC, transaction, revert or successful onchain action is simulated as real.
